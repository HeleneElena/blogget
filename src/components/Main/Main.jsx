import { Layout } from '../Layout/Layout';
import style from './Main.module.css';

export const Main = () => {
    return (
        <main className={style.main}>
           <Layout>
            
           </Layout>
        </main>
    )
}